<footer>
  <div>
    <p class="sr-only">Neurosoft.dev</p>
  </div>
  <p id="footer"></p>
  <aside>
    <a href="https://github.com/EdwinEspi18/neurosoft.git" target="_blank" rel="noopener noreferrer">
      <img src="/logo-github.svg" alt="github" loading="lazy" />
    </a>
    <a href="#" target="_blank" rel="noopener noreferrer">
      <img src="/logo-linkedin.svg" alt="linkedin" loading="lazy" />
    </a>
    <a href="#" target="_blank" rel="noopener noreferrer">
      <img src="/logo-ig.svg" alt="linkedin" loading="lazy" />
    </a>
    <a href="#" target="_blank" rel="noopener noreferrer">
      <img src="/logo-facebook.svg" alt="linkedin" loading="lazy" />
    </a>
  </aside>
</footer>

<style>
footer {
  display: flex;
  justify-content: space-between; /* Cambiar a space-between para mejorar la distribución */
  align-items: center;
  gap: 1rem;
  background-color: white;
  padding: 1rem 2rem; /* Añadir padding para mejor espaciado */
  flex-wrap: wrap; /* Permitir que los elementos se ajusten en varias líneas en pantallas pequeñas */
}

p {
  font-weight: 700;
  font-size: 20px; /* Ajustar tamaño de fuente */
  color: rgb(var(--primary));
}

aside {
  display: flex;
  align-items: center;
  gap: 1rem;
}

aside img {
  height: 30px;
  cursor: pointer;
}

/* Media Queries para hacer el footer responsive */

/* Ajustes para tablets */
@media (max-width: 1024px) {
  footer {
    flex-direction: column; /* Cambiar a disposición vertical en tablets */
    gap: 1.5rem; /* Aumentar el espacio entre elementos */
  }

  aside {
    justify-content: center; /* Centrar los íconos sociales */
  }

  p {
    font-size: 18px; /* Reducir tamaño de fuente */
  }
}

/* Ajustes para pantallas pequeñas */
@media (max-width: 768px) {
  footer {
    flex-direction: column; /* Disposición vertical en pantallas pequeñas */
    align-items: center;
    padding: 1.5rem;
  }

  p {
    font-size: 16px; /* Reducir aún más el tamaño de la fuente */
    text-align: center;
  }

  aside {
    justify-content: center; /* Centrar los íconos sociales */
  }

  aside img {
    height: 25px; /* Reducir el tamaño de los íconos */
  }
}
.sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border-width: 0;
  }
/* Ajustes para pantallas muy pequeñas (móviles pequeños) */
@media (max-width: 480px) {
  footer {
    padding: 1rem;
  }

  p {
    font-size: 14px;
    text-align: center;
  }

  aside img {
    height: 20px; /* Reducir el tamaño de los íconos en pantallas muy pequeñas */
  }
}

</style>

<script>
  document.getElementById('footer')!.innerHTML = `© neurosoft.dev ${new Date().getFullYear()}`;
</script>